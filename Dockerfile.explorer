FROM python:3.13-slim

WORKDIR /app

COPY pyproject.toml .
COPY task_manager_mcp/ task_manager_mcp/
RUN pip install --no-cache-dir -e ".[explorer]"

ENV OBSIDIAN_VAULT_PATH=/vault

EXPOSE 8765

ENTRYPOINT ["python", "-m", "task_manager_mcp.explorer", "--host", "0.0.0.0", "--port", "8765"]
